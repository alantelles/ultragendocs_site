Router = $app.uses.Router
Request = $app.uses.Request
AppResponse = $app.uses.AppResponse
ApiClient = $app.uses.ApiClient

Base = $app.templates.Base
MinimalBase = $app.templates.MinimalBase

$router = $app.routers.main
$docs = $app.routers.docs
$apiRef = $app.routers.apiRef

$response = new AppResponse()

$router.getMatch($request[:route], $request[:method]).localize()

live handler(params)